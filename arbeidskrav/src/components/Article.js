export default function Article (data) {
    return ( 
<></>
      /* .map((title, url, category) => (
      { <article className="innhold">
            <h3>{cat}</h3>
              <ul>
                <li key={title} />
                <a key={url} />
              </ul
        </article> }
          
        } */
      )
}