export default function Article () {
    return ( 

      //.map((title, url, cat) => (
      {/* <article className="innhold">
            <h3>{cat}</h3>
              <ul>
                <li key={title} />
                <a key={url} />
              </ul
        </article> */}
          
      //  }
      )
}